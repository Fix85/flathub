app-id: com.paradoxclient.ParadoxClient
runtime: org.freedesktop.Platform
runtime-version: '26.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '26.08'
command: paradox-client
separate-locales: false

finish-args:
  - --share=network
  - --share=ipc
  - --device=dri
  - --socket=x11
  - --socket=pulseaudio
  - --filesystem=xdg-run/app/com.discordapp.Discord:create
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=org.freedesktop.Notifications

modules:
  - name: xrandr
    sources:
      - type: archive
        url: https://www.x.org/archive/individual/app/xrandr-1.5.1.tar.gz
        sha256: 7b99edb7970a1365eaf5bcaf552144e4dfc3ccf510c4abc08569849929fb366e

  - name: paradox-client
    buildsystem: simple
    sources:
      - type: file
        url: https://paradoxclient.com/downloads/launcher/Paradox-Client-0.1.34-x86_64.AppImage
        sha256: ed2661325b85338a50a157a9a50f314e3c1dea856648fac346497aaaddfc5f3a
        dest-filename: paradox-client.appimage
        x-checker-data:
          type: html
          url: https://paradoxclient.com/api/launcher/latest-linux.yml
          version-pattern: 'version:\s*([\d.]+)'
          url-template: https://paradoxclient.com/downloads/launcher/Paradox-Client-$version-x86_64.AppImage

      - type: script
        dest-filename: paradox-client
        commands:
          - export HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
          - exec zypak-wrapper /app/paradox-client/paradox-launcher "$@"

    build-commands:
      - chmod +x ./paradox-client.appimage
      - ./paradox-client.appimage --appimage-extract
      - mv squashfs-root "${FLATPAK_DEST}/paradox-client"
      - install -Dm755 paradox-client "${FLATPAK_DEST}/bin/paradox-client"
      - install -Dm644 "${FLATPAK_DEST}/paradox-client/usr/share/applications/${FLATPAK_ID}.desktop" -t "${FLATPAK_DEST}/share/applications"
      - install -Dm644 "${FLATPAK_DEST}/paradox-client/usr/share/metainfo/${FLATPAK_ID}.metainfo.xml" -t "${FLATPAK_DEST}/share/metainfo"
      - install -Dm644 "${FLATPAK_DEST}/paradox-client/usr/share/icons/hicolor/512x512/apps/${FLATPAK_ID}.png" -t "${FLATPAK_DEST}/share/icons/hicolor/512x512/apps"
